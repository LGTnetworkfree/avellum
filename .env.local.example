# ==============================================================================
# Avellum Environment Variables
# ==============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# ==============================================================================
# Supabase Configuration (Required)
# ==============================================================================
# Get these from your Supabase project dashboard: Settings > API

# Public URL - safe to expose to client
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anonymous key - safe to expose to client (used for public operations)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key - KEEP SECRET (server-side only)
# This key bypasses RLS and should never be exposed to the client
# Required for all API routes to function
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ==============================================================================
# Solana / Helius Configuration (Required)
# ==============================================================================
# Helius API key for reliable RPC access
# Get one at: https://www.helius.dev/
HELIUS_API_KEY=your-helius-api-key

# $AVELLUM Token Mint Address (Solana SPL Token)
# This is the on-chain address of the AVLM token
NEXT_PUBLIC_AVLM_TOKEN_MINT=D6zGvr8zNKgqpcjNr4Hin8ELVuGEcySyRn5ugHcusQh9

# Solana Network
# Options: mainnet-beta, devnet, testnet
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta

# ==============================================================================
# Application Configuration (Optional)
# ==============================================================================
# Node environment (development, production, test)
NODE_ENV=development

# ==============================================================================
# Rate Limiting (Optional - defaults configured in code)
# ==============================================================================
# Uncomment and modify to override default rate limits
# RATE_LIMIT_RATE_MAX=10
# RATE_LIMIT_RATE_WINDOW_MS=60000
# RATE_LIMIT_AGENTS_MAX=30
# RATE_LIMIT_AGENTS_WINDOW_MS=60000

# ==============================================================================
# Security Notes
# ==============================================================================
# 1. SUPABASE_SERVICE_ROLE_KEY must never be exposed to the client
# 2. All NEXT_PUBLIC_* variables are bundled into client-side code
# 3. RLS (Row Level Security) is enabled - service role key bypasses it
# 4. API routes validate all input - see /lib/validation.ts
# 5. Rate limiting is applied per IP - see /lib/rate-limit.ts
